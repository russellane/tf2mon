"""Application control."""


class Control:
    """Application control."""

    UI = None

    # def bind(self, keyspec: str, game_only: bool = False) -> None:
    #     """Bind `Command` returned by `cmd_factory` with `keyspec`."""

    #     # self.commands.bind(self.command, keyspec, game_only)
